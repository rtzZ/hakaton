import { PaperLayout } from "../../../shared/layout";
import {PageWrapper} from "../../pageWrapper";
import { Loading } from "../../../entities/loading/ui/Loading";

export const LoadingPage = () => {
    return (
        <PageWrapper>
            <Loading/>
        </PageWrapper>
    )
}